namespace CScharpConsoleApp.src.Basic
{
    public enum Month : Byte // can type setted only num(default is int)
    {
        Jan = 2, // can also be setted
        Feb = 12,
        Mar,
        Apr,
        May,
        Jun,
        Jul,
        Aug,
        Sep,
        Oct,
        Nov,
        Dec
    }
}